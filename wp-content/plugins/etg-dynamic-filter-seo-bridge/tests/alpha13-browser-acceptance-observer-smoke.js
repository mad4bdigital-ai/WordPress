'use strict';
const assert=require('assert'),fs=require('fs'),path=require('path'),vm=require('vm'),nodeCrypto=require('crypto');
const {TextEncoder}=require('util');
const root=path.resolve(__dirname,'..');
const source=fs.readFileSync(path.join(root,'assets/js/browser-acceptance-observer.js'),'utf8');
function sleep(ms){return new Promise(r=>setTimeout(r,ms));}
function node(attrs={},text=''){return{attrs:Object.assign({},attrs),textContent:text,getAttribute(n){return Object.prototype.hasOwnProperty.call(this.attrs,n)?this.attrs[n]:null;}}}
(async()=>{
  let ids=[1,2],count=2;const listeners={},subs={};
  const canonical=node({href:'https://staging.egypttourgates.com/tours-and-activities/'}),robots=node({content:'index, follow'}),description=node({content:'Tours'}),hreflang=[node({hreflang:'en',href:'https://staging.egypttourgates.com/tours-and-activities/'}),node({hreflang:'ar',href:'https://staging.egypttourgates.com/ar/tours-and-activities/'})];
  const document={title:'Tours',querySelector(sel){if(sel==='link[rel="canonical"]')return canonical;if(sel==='meta[name="robots"]')return robots;if(sel==='meta[name="description"]')return description;if(sel.indexOf('results-count')!==-1||sel==='[data-etg-dfsb-result-count]')return node({'data-etg-dfsb-result-count':String(count)},String(count));return null;},querySelectorAll(sel){if(sel==='link[rel="alternate"][hreflang]')return hreflang;if(sel.indexOf('data-post-id')!==-1)return ids.map(id=>node({'data-post-id':String(id)}));return[];},addEventListener(name,cb){(listeners[name]=listeners[name]||[]).push(cb);},dispatchEvent(event){(listeners[event.type]||[]).forEach(cb=>cb(event));return true;}};
  const originalFetch=(url,opts)=>Promise.resolve({status:200,clone(){return{text:()=>Promise.resolve(JSON.stringify({contract:'etg.dfsb.ajax-presentation.v1',status:'ready',authorizing:false,url_authority:false,seo_mutation:false,provider:'jet-engine',query_id:'tours_query_archive'}))}}});
  const history={pushState(){},replaceState(){}};
  let perfNow=1000;
  const performance={now(){perfNow+=25;return perfNow;},getEntriesByType(type){return type==='navigation'?[{startTime:0,requestStart:10,responseStart:60,domContentLoadedEventEnd:180,loadEventEnd:220}]:[];}};
  const window={performance,crypto:nodeCrypto.webcrypto,TextEncoder,location:{href:'https://staging.egypttourgates.com/tours-and-activities/',pathname:'/tours-and-activities/'},history,fetch:originalFetch,JetSmartFilters:{filterGroups:{'jet-engine/tours_query_archive':{currentQuery:{}}},events:{subscribe(name,cb){subs[name]=cb;}}},setTimeout,clearTimeout};
  const context={window,document,URL,JSON,Array,String,Number,Error,RegExp,parseInt,isFinite,setTimeout,clearTimeout,console,Uint8Array,Promise};
  vm.runInNewContext(source,context,{filename:'browser-acceptance-observer.js'});
  const observer=window.ETGDFSBBrowserAcceptanceObserver;
  assert(observer,'observer must expose a bounded API');assert.strictEqual(observer.contract,'etg.dfsb.browser-acceptance-observer.v1');assert.strictEqual(observer.passive,true);assert.strictEqual(observer.authorizing,false);
  assert.strictEqual(observer.arm({case_id:'x',provider:'bad/provider',query_id:'q',taxonomy:'t',term_slug:'s'},{nonce:'0123456789abcdef0123456789abcdef'}).ok,false,'arbitrary provider input must fail closed');
  const planCase={case_id:'case-luxor',provider:'jet-engine',query_id:'tours_query_archive',taxonomy:'location_jet',term_slug:'luxor'};
  assert.strictEqual(observer.arm(planCase).ok,false,'missing freshness challenge must fail closed');
  assert.strictEqual(observer.arm(planCase,{nonce:'bad'}).ok,false,'malformed freshness nonce must fail closed');
  const challenge={nonce:'0123456789abcdef0123456789abcdef'};
  const armed=observer.arm(planCase,challenge);assert.strictEqual(armed.ok,true);assert.strictEqual(armed.authorizing,false);assert.strictEqual(armed.challenge_nonce,challenge.nonce);
  await sleep(5);assert(subs['ajaxFilters/updated'],'observer must subscribe to the real JetSmartFilters event');
  for(let i=0;i<40;i+=1){window.history.pushState({},'', '/noop-'+i);document.dispatchEvent({type:'etg-dfsb/ajax-presentation-blocked',detail:{reason:'r'.repeat(200),group:'jet-engine/tours_query_archive',blocking_reasons:Array.from({length:40},(_,j)=>'reason-'+j),unknown:'must-drop'}});}
  ids=[31,32,33];count=3;window.location.href='https://staging.egypttourgates.com/tours-and-activities/jsf/jet-engine:tours_query_archive/tax/location_jet:luxor/';window.location.pathname='/tours-and-activities/jsf/jet-engine:tours_query_archive/tax/location_jet:luxor/';
  subs['ajaxFilters/updated']('jet-engine','tours_query_archive');
  await window.fetch('https://staging.egypttourgates.com/wp-json/etg-dfsb/v1/ajax-presentation',{method:'POST'});await sleep(5);
  document.dispatchEvent({type:'etg-dfsb/ajax-presentation-updated',detail:{provider:'jet-engine',query_id:'tours_query_archive'}});await sleep(5);
  ids=[1,2];count=2;window.location.href='https://staging.egypttourgates.com/tours-and-activities/';window.location.pathname='/tours-and-activities/';
  document.dispatchEvent({type:'etg-dfsb/ajax-presentation-reset',detail:{restored_initial:true}});await sleep(5);
  const evidence=observer.snapshot();
  assert.strictEqual(evidence.contract,'etg.dfsb.browser-acceptance-observer.v1');assert.strictEqual(evidence.passive,true);assert.strictEqual(evidence.authorizing,false);assert.strictEqual(evidence.challenge_nonce,challenge.nonce,'snapshot must echo the armed freshness nonce');
  assert.strictEqual(evidence.runtime.javascript_runtime,true);assert.strictEqual(evidence.runtime.jet_smart_filters_observed,true);assert.strictEqual(evidence.runtime.filter_group,'jet-engine/tours_query_archive');
  assert.deepStrictEqual(Array.from(evidence.rendered.ids),[31,32,33]);assert.strictEqual(evidence.rendered.result_count,3,'filtered DOM snapshot must survive later reset');
  assert.strictEqual(evidence.events.ajax_filters_updated,true);assert.strictEqual(evidence.events.presentation_updated,true);assert.strictEqual(evidence.events.presentation_reset,true);
  assert.strictEqual(evidence.performance.ttfb_ms,50);assert(evidence.performance.ajax_endpoint_latency_ms>=0);assert(evidence.performance.filter_to_presentation_ms>=0);
  assert.strictEqual(evidence.network.method,'POST');assert.strictEqual(evidence.network.http_status,200);assert.strictEqual(evidence.network.contract,'etg.dfsb.ajax-presentation.v1');assert.strictEqual(evidence.network.authorizing,false);assert.strictEqual(evidence.network.url_authority,false);assert.strictEqual(evidence.network.seo_mutation,false);
  assert.strictEqual(evidence.url_state.filter_state_observed,true);assert.strictEqual(evidence.url_state.etg_history_mutation,false);assert(evidence.url_state.filtered_url.length<=2048);assert(evidence.url_state.reset_url.length<=2048);
  assert.strictEqual(evidence.seo.canonical_unchanged,true);assert.strictEqual(evidence.seo.robots_unchanged,true);assert.strictEqual(evidence.seo.hreflang_unchanged,true);assert.strictEqual(evidence.seo.rank_math_unchanged,true);
  assert.strictEqual(evidence.reset.event_observed,true);assert.strictEqual(evidence.reset.neutral_state_restored,true);
  assert.strictEqual(evidence.history_calls.length,32,'history diagnostics must be capped');assert(evidence.history_calls.every(item=>item.method==='pushState'&&item.etg_source===false),'history diagnostics must stay bounded and non-authorizing');
  assert.strictEqual(evidence.blocked_events.length,32,'blocked diagnostics must be capped');assert(evidence.blocked_events.every(item=>item.reason.length===160),'blocked reason strings must be bounded');assert(evidence.blocked_events.every(item=>item.blocking_reasons.length===32),'blocked reason arrays must be bounded');assert(evidence.blocked_events.every(item=>!Object.prototype.hasOwnProperty.call(item,'unknown')),'blocked diagnostics must drop arbitrary fields');
  assert.strictEqual(Object.prototype.hasOwnProperty.call(evidence.rendered,'proof_ids'),false,'raw full proof IDs must not cross the evidence boundary');
  observer.disarm();
  ids=Array.from({length:150},(_,i)=>i+1);count=150;window.location.href='https://staging.egypttourgates.com/tours-and-activities/jsf/jet-engine:tours_query_archive/tax/location_jet:luxor/';window.location.pathname='/tours-and-activities/jsf/jet-engine:tours_query_archive/tax/location_jet:luxor/';
  assert.strictEqual(observer.arm(planCase,challenge).ok,true,'large-result case re-arms');
  subs['ajaxFilters/updated']('jet-engine','tours_query_archive');
  document.dispatchEvent({type:'etg-dfsb/ajax-presentation-updated',detail:{provider:'jet-engine',query_id:'tours_query_archive'}});await sleep(5);
  const largeEvidence=await observer.snapshotAsync();
  const canonical=v=>JSON.stringify(v);
  const hex=v=>nodeCrypto.createHash('sha256').update(canonical(v)).digest('hex');
  assert.strictEqual(largeEvidence.rendered.ids.length,100,'sync evidence stays capped at 100 IDs');
  assert.strictEqual(largeEvidence.rendered.observed_id_count,150,'observer records full bounded DOM count');
  assert.strictEqual(largeEvidence.rendered.digest_authoritative,true,'async snapshot produces authoritative large-result digest');
  assert.strictEqual(largeEvidence.rendered.proof_item_count,150,'async digest binds the full DOM proof count');
  assert.strictEqual(largeEvidence.rendered.identity_digest,hex(ids.slice().sort((a,b)=>a-b)),'identity digest matches canonical semantic proof');
  assert.strictEqual(largeEvidence.rendered.order_digest,hex(ids),'order digest matches canonical semantic proof');
  assert.strictEqual(Object.prototype.hasOwnProperty.call(largeEvidence.rendered,'proof_ids'),false,'async evidence does not leak raw full proof IDs');
  observer.disarm();assert.strictEqual(window.fetch,originalFetch,'observer restores fetch instrumentation on disarm');
  assert(!source.includes('.click('),'observer must not drive UI clicks');assert(!source.includes('location.href ='),'observer must not navigate the browser');assert(!source.includes('history.pushState('),'observer must not create history state');
  console.log('Alpha13 passive browser acceptance observer smoke tests passed.');
})().catch(e=>{console.error(e.stack||e);process.exit(1)});
