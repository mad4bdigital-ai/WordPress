# Combination Registry Contract v1

Exact deterministic signature: `<language>|<taxonomy>=<slug>|...` with taxonomy keys sorted. Pair/triple and tour-type-only states require exact approval by default. Registry has no wildcard semantics. Result count does not replace registry approval.
