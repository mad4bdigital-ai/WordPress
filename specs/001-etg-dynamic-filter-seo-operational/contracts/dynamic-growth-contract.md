# Dynamic Growth Contract v2

`etg.dfsb.dynamic-growth.v1`

Growth is explicit and monotonic in authority:
`discover → disabled blueprint → exact profile configuration → simulation → readiness → staging evidence → explicit enable`.

Discovery, traffic, crawler demand, or generated blueprints never enable a profile or approve taxonomy sets/combinations. Alpha bounds fail visibly rather than silently truncating authority.
