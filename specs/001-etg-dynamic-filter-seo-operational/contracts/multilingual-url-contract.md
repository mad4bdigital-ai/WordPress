# Multilingual URL Contract v1

For explicit preview URLs, first path segment matching `wpml_active_languages` determines language; otherwise WPML default/current language applies. Archive canonical uses parsed `archive_path`, preserving prefixes such as `/it/`. Missing term translation is a hard indexing deny. Non-ar/en generated connective text is neutral unless localized via filters.
